import React from 'react';

const Home = () => {
    return (
        <header style={{ textTransform: "uppercase", fontSize: "2rem" }}>
            # Welcome to my pet shop #
        </header>
    )
}

export default Home;